export type IReview = [string, number, string];

export enum IGenre {
  Fantasy = 'fantasy',
  Adventure = 'adventure',
  Horror = 'horror',
}

export enum Cover {
  Paperback = 'paperback',
  Hardcover = 'hardcover',
}
